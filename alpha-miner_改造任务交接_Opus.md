---
title: alpha-miner 改造任务交接（for Claude Code / Opus）
type: 任务交接 / handoff
created: 2026-06-08
target_model: Opus（在 Claude Code 中，直接读写仓库）
repo: github.com/Heptyl/alpha-miner
---

# alpha-miner 改造任务交接（for Claude Code / Opus）

> 读者是 Claude Code 里的 Opus 实例，拥有本仓库的直接文件读写权限。本文件承载一系列策略讨论得出的决策与文件锚点。**先审计、后确认、最后才动代码**，不要读完就直接改。

---

## 0. 背景与铁律

- 使用者是资深 A 股短线交易者（打板/连板方向），工程师出身，懂行为金融，不需要科普。
- **物理现实优先于推断**：任何涉及真实数据/真实行情的判断，数据未知就问，不要外推。
- 本仓库已运行一段时间（29 commits，105 tests），**README 可能滞后于代码**。一切以你实际读到的源码为准。
- 沟通要求：高信息密度，不要奉承，区分"事实/推断"，不替使用者做交易决策。

---

## 1. 第一步：审计（只读，禁止改动）

按顺序读以下文件，产出一份《现状审计报告》。**这一步不准写任何代码。**

必读文件：

1. `src/mining/evolution.py` — 确认进化引擎当前的**验收门**到底是什么（README 说是 `Spearman IC > 0.03 且 ICIR > 0.5`，核实代码真实逻辑）。
2. `src/drift/ic_tracker.py` — 确认它实际输出哪些指标（README 说已含 胜率/盈亏比，核实），滚动窗口默认长度是多少。
3. `src/drift/regime.py` — 确认当前 regime 分类维度，以及判定用到哪些信号/因子。
4. `src/factors/narrative/leader_clarity.py` — 确认"龙头清晰度"的精确计算口径（是否=题材内第一名/第二名成交额）。
5. `src/factors/narrative/theme_crowding.py` — 确认拥挤度口径，它现在是 alpha 还是 filter 角色。
6. `cli/backtest.py` — 确认回测当前输出的是 IC 还是收益分布，能否按因子组合（非单因子）回测。
7. `src/data/storage.py` + `src/data/schema.sql` — 确认 DB 是否真的为空、有哪些表已有数据、时间隔离逻辑。
8. `knowledge_base/theories.yaml` — 确认理论库结构，判断"叙事母题/原型"能否挂进来。

审计报告需明确回答：

- 进化引擎验收门的**真实**逻辑（贴关键代码行）。
- `ic_tracker` 是否已能产出"满足条件样本的 T+1/T+3/T+5 收益分布 + 胜率 + 盈亏比"，还是只有横截面 IC。
- DB 当前真实数据量（哪些表有多少行，时间范围）。**这一条决定后面所有任务能不能跑。**
- regime 判定是否已用到 `leader_clarity`。

---

## 2. 已确定的方向性决策（审计后逐项与使用者确认，再实施）

以下是策略讨论得出的结论，不是让你自由发挥，但实施前要先用审计结果验证是否仍适用。

### 决策 A：进化引擎验收门 从 IC 改为事件研究
- **锚点**：`src/mining/evolution.py`、`src/mining/prompts/*.md`
- **原因**：A 股短线打板属"事件交易 + 微观结构"，不是传统截面 alpha 挖掘。`IC>0.03 且 ICIR>0.5` 在短窗口 + 小样本（连板票每天仅几十只）下会被多重检验偶然击穿，结构上是过拟合生产线。
- **改法**：
  - 验收指标：IC/ICIR → **事件研究胜率 + 盈亏比**（满足因子组合条件的样本，买入后 T+1 为主、T+3/T+5 为辅的收益分布）。
  - 验收门槛：→ **近 60 日胜率 > 55% 且 近 30 日胜率 > 55%（两段都过）**，单段达标不算。
  - 进化对象：从"挖新因子" → **"进化现有因子组合的触发条件"**（9 因子是原料，进化调配比不发明新原料）。
- **注意**：`ic_tracker.py` 可能已有胜率/盈亏比，优先复用，不要新造重复模块。

### 决策 B：新增 regime 轴 —— 游资主导 vs 量化主导
- **锚点**：`src/drift/regime.py`，输入复用 `leader_clarity` + 新增"连板高度衰减斜率"
- **原因**：2026 年量化定价权扩张 + 4 月新规（限频/撤单率≤15%/通道平权）落地后，连板环境两极分化。同一因子组合在"游资主导"和"量化主导"下胜率分布完全不同，混在一起算平均数没有意义。
- **改法**：在现有情绪 regime 之外，新增一个正交的"定价权 regime"标签：
  - 判别特征：龙头唯一性（`leader_clarity` 高=游资造龙头清晰）+ 连板高度衰减斜率（持续走低=量化套利主导）。
  - 输出：每个交易日打一个 `pricing_regime ∈ {hot_money_led, quant_led, mixed}` 标签，写入 `regime_state` 表。
- **下游影响**：决策 A 的事件研究胜率，必须**按 pricing_regime 分层统计**。

### 决策 C：事件研究法落地到 backtest
- **锚点**：`cli/backtest.py`
- **改法**：新增（或改造）一个回测模式，输入"因子组合触发条件"，输出该条件命中样本的 T+1/T+3/T+5 收益分布、胜率、盈亏比、最大回撤，并把过去 60 日切 3 段（每段 20 日）看稳定性。
- **收益窗口重心放 T+1**：龙头加冕逻辑弱化、连板高度压缩后，持板过夜尾部风险变大。

### 决策 D：叙事母题/原型 作为人工标注层
- **锚点**：`knowledge_base/theories.yaml`、`src/factors/narrative/`、`src/narrative/news_classifier.py`
- **背景**：另有一份《龙头叙事解剖_GLM分析任务包》产出的龙头案例库（华电辽能=主线趋势型、合富中国=游资玄学型 等），每只标注了"叙事母题 + 谢林点来源（人为/自然）+ 量化盲区特征"。
- **改法（待 GLM 产出后再做，本次先留接口）**：
  - 把"叙事母题"作为可标注维度挂进知识库或新增一张表。
  - 玄学/谐音/两岸母题这类**量化无法定价的特征**，明确标记为"人工供给特征"，不要试图自动因子化——这是 edge 的来源，不是要消除的噪音。
  - 拥挤度/同质化特征定位为 **风控 filter（否决项）**，不是 alpha 来源。

---

## 3. 真正的阻塞项：DB 为空

README "待完成"列着"因子 IC 实盘验证（当前 DB 空，需数据积累）"。**如果审计确认 DB 仍为空，则决策 A/B/C 全部无法验证，只能写代码不能出结论。**

优先级排序：
1. 先解决数据填充（`cli/collect.py` + `scripts/daily_run.sh`），把近 3–6 个月（不跨重大 regime 切换点）的 akshare 数据采全。长历史对短线打板是负资产，不要回填 2–3 年。
2. 数据到位后，先跑决策 C 的事件研究，产出"当下 9 因子哪几个还活着"的第一份体检报告。
3. 体检报告出来再动决策 A/B。**顺序不可颠倒。**

---

## 4. 约束 / 不要做的事

- 不要新增重复模块（先查 `ic_tracker` / `leader_clarity` / `theme_crowding` 是否已覆盖）。
- 不要回填长历史数据。
- 不要把进化引擎改成"自动变强"的黑箱——LLM 出实现、使用者出假说，验证流程严格留人工闸门。
- 不要把仓位映射表的上限（README 现为 强=80%）当默认；使用者刚经历重大亏损，重启期上限压到 40%。
- 改动前先给 diff 计划，确认后再写。

---

## 5. 本次交互流程

1. 你先产出第 1 节《现状审计报告》。
2. 使用者据此确认决策 A–D 哪几项本轮做、按什么顺序。
3. 确认后你给逐项 diff 计划。
4. 再写代码。
