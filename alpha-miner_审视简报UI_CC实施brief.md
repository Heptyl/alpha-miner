---
title: alpha-miner 每日审视简报 UI — Claude Code 实施 brief
type: 实施任务 / handoff
created: 2026-06-11
target: Opus（Claude Code，仓库内直接读写）
repo: github.com/Heptyl/alpha-miner
---

# alpha-miner 每日审视简报 UI — 实施 brief

> 给 Claude Code 中 Opus 的完整实施指令。使用者不会逐行审你的代码,
> 你的产出靠「行为验收标准(第 6 节)」验收,不靠代码审查。
> 先做第 0 步审计并输出结果,经使用者确认后再写代码。

---

## 1. 背景与设计哲学(决定所有取舍,违反即返工)

使用者处境:仓库代码绝大部分由 AI 编写,使用者只掌握整体框架,
不再逐行阅读实现。本 UI 的唯一目的:**让使用者每天花 ≤5 分钟,
不读任何代码,就知道系统是否健康、有什么事需要他决策。**

由此推出的铁律:

- **UI 本身不能成为新的债**。禁止引入 Web 框架、React、npm 构建链、
  常驻服务进程。整个 UI = 一个 Python 脚本 + 一个 Jinja2 模板,
  产出一张自包含的静态 HTML。坏了就重新生成,不修不调。
- **只读**。v1 不提供任何写操作按钮。所有需要使用者决策的事项,
  在简报中直接印出对应的 CLI 命令(复制即可执行),由现有 CLI 承担写入。
  UI 永远不碰钱、不改状态。
- **静默为常,异常才响**。一切正常时简报接近空白(绿色快照)。
  只有异常和待决策事项才占版面。
- **诚实优先**。数据缺失就大字显示「数据缺失」,禁止用占位假数据填充。
  DB 为空时,简报的正确形态就是一张写着「DB 为空,采集未运行」的红色页面。

---

## 2. 第 0 步:信号链路审计(先做,只读,输出后暂停等确认)

写代码前,先回答以下问题并把结论汇报给使用者:

1. **信号生成实际消费哪些表?** 从买卖信号/选股的入口函数出发,
   追踪它实际 SELECT 的表清单。
2. **信息瀑布/叙事类因子的真实数据源是什么?**
   消费的是新闻文本(news 相关表),还是量价/龙虎榜行为数据?
   这直接决定该因子当前是否坐在低质量数据源上。
3. **DB 各表的真实数据量**(行数、时间范围)。
4. **现有 `daily_run.sh` / `cli/collect.py` 的产出物清单**,
   确认简报脚本能挂在哪个环节之后。

审计结论将作为简报「数据源健康」区块的初始内容。
**输出审计结果后停下,等使用者确认,再进入第 3 步。**

---

## 3. 交付物

```
scripts/generate_brief.py      # 唯一脚本:读 SQLite → 渲染 HTML
templates/brief.html.j2        # Jinja2 模板(单文件,内联 CSS,无外部资源)
reports/brief/YYYY-MM-DD.html  # 每日产出,自包含,双击可开
reports/brief/latest.html      # 永远指向最新一期(覆盖写)
tests/test_brief.py            # 行为测试(见第 6 节)
```

- 依赖只允许:Python 标准库 + jinja2 + 仓库已有依赖。新增依赖需说明理由。
- 挂载:在 `daily_run.sh` 末尾追加一行调用 `generate_brief.py`。
- HTML 必须自包含:无 CDN、无外部 JS/CSS/字体,断网可开。
  允许少量内联 JavaScript 用于折叠/展开,但页面在 JS 禁用时仍可完整阅读。

---

## 4. 简报内容结构(自上而下,严格按此顺序)

### 4.0 头部
日期、距上次生成的间隔、一句话总结:
「N 项待你处理 · 其余 M 项已自动验证放过」。
N 的构成见 4.2,M = 本期扫描到但未达到提醒阈值的事件数。

### 4.1 数据源健康(常驻,放最顶因为它决定下面一切可信度)
- 各关键表:行数、最新数据日期、距今天数。
- 采集脚本上次运行时间与退出状态(从日志或产出物时间戳推断)。
- 第 0 步审计发现的「信号链路 → 数据源」映射,
  特别标注:瀑布/叙事因子当前消费的数据源及其质量评级。
- 任一关键表数据滞后 > 1 个交易日 → 该区块整体变红,置顶警告。

### 4.2 需要你的注意(待决策区,最多 5 张卡,超出截断并注明)
事件来源(按现有 DB 能力实现,没有的就先不做并注明):
- **进化引擎产出待放行的候选因子**(若 mining_log 或同等表有记录):
  卡片含:因子名、假说一句话、回测胜率/盈亏比/样本数、
  自动预检标记(见 4.5)、以及对应的 CLI 放行/驳回命令(印出来,可复制)。
- **因子漂移告警**(若 ic_tracker 有滚动指标):
  近 20 日胜率相对近 60 日下滑超过阈值(默认 8 个百分点,做成配置)。
- **回测/采集任务失败**。

每张卡固定三段式:「做了什么 / 为什么·观测到什么 / 你可以做什么(CLI 命令)」。
禁止在卡片里贴代码。

### 4.3 系统行为快照(一行小卡,绿色为常态)
今日 regime 标签、存活因子数(按近 60 日胜率 > 55% 计)/总因子数、
测试通过数(读最近一次 pytest 产出,若无则显示「未运行」)、数据新鲜度。

### 4.4 风控常驻脚注
仓位上限 40%(重启期)· 所有买卖信号需人工确认。写死在模板里。

### 4.5 自动预检规则(v1 只做这三条,宁缺毋滥)
对待放行因子打标记:
1. **样本量**:回测样本 < 100 → 黄旗「样本不足」。
2. **因子冗余**:与现有因子收益序列相关性 > 0.6 → 黄旗「疑似换皮」,标出对象。
3. **三段稳定性**:近 60 日切 3 段(各 20 日),任一段胜率 < 50% → 黄旗「不稳定」。
regime 分层预检 v1 不做(依赖尚不存在的 pricing_regime 标签),留 TODO 注释。

---

## 5. 不要做的事

- 不做 Web 服务、不做实时刷新、不做任何交互式写操作。
- 不做进化监控屏、变更流、系统地图等其余屏——v1 只有这一张简报。
- 不在简报中展示任何代码片段或 diff。
- DB 缺数据时不造假数据、不静默跳过区块——显式标注缺失。
- 不重构现有模块来「配合」简报。简报只读现状,现状不够就如实显示不够。

---

## 6. 行为验收标准(使用者按此验收,共 5 条,写成 pytest)

1. **空库测试**:对一个空 SQLite 运行脚本 → 正常退出,
   产出的 HTML 含「数据缺失」红色警告,不抛异常、不出现假数据。
2. **正常日测试**:对一个含完整 fixture 数据的库运行 →
   HTML 含 4.1–4.4 全部区块,且「待处理」计数与 fixture 中事件数一致。
3. **漂移触发测试**:fixture 中构造一个胜率下滑 10 个百分点的因子 →
   该因子必须出现在 4.2 待决策区。
4. **预检测试**:fixture 中构造一个样本量 80 的候选因子 →
   必须带「样本不足」黄旗。
5. **自包含测试**:产出的 HTML 中不得出现 http:// 或 https:// 的资源引用。

---

## 7. 实施顺序

1. 第 0 步审计 → 汇报 → **暂停等确认**。
2. 写 5 条行为测试(先红)。
3. 写脚本与模板,测试转绿。
4. 挂入 daily_run.sh,生成第一期真实简报,交付 latest.html 路径。
