# Alpha Miner 项目状态看板

> 面向 PM / 项目负责人。状态日期：2026-08-14。只在结论、证据、风险或优先级变化时更新。

## 一句话结论

基础工程、数据发布和远程计算已经可运行；涨停专项已经能产生“因子结构 → 次日条件 → 退出 → 仓位”的操作卡，
但历史样本只有 16 个可用信号日，最新候选未通过锁定测试，当前产品状态是**研究/观察可用，实盘不可用**。

## 当前进度

| 模块 | 状态 | 可验证证据 | PM 结论 |
|------|------|------------|---------|
| 当日数据采集 | 已完成 | 2026-08-14 本地采集 13,404 行，约 31 秒 | 可日常使用 |
| 数据正确性 | 已修复 | 腾讯成交量/成交额字段与日期校验；历史采集隔离实时源 | 继续做日常抽查 |
| 数据发布 | 已完成 | SQLite backup 校验、原子替换、保留 previous | Windows→服务器链路可用 |
| 远程运行时 | 已完成 | Ubuntu 18.04 离线 Python 3.12；涨停专项远端 5/5 通过 | 计算可放服务器 |
| 通用 IC 进化 | 可续跑 | checkpoint、并发、失败者复苏已完成 | 尚无准入因子 |
| 涨停结构因子 | 已实现 | 5 个注册因子、10 基因结构进化、可读公式 | 研究可用 |
| 涨停操作卡 | 已实现 | 入场、退出、仓位、贡献、WATCH/AVOID 闸门 | 当前只能观察 |
| 用户入口 | 已实现 | `python -m cli zt daily/status/scan` | 日常只需 1 个主命令 |
| Git 分离交付 | 本轮完成三批 | 数据 `ef40a00`；计算 `e5e2e96`；涨停产品为本次独立提交 | 可分别回退 |

远程同步已修复 robocopy 裸 `data` 排除规则误伤 `src/data` 的问题；排除目录现在锚定项目根路径，
避免服务器同时存在“新计算代码 + 旧 schema”的半更新状态。

## 最新涨停专项演化结果

2026-08-14 使用 5 代 × 24 候选运行。数据集为 16 个有效信号日、1,008 个涨停事件。

| 指标 | 训练 | 验证 | 锁定测试 |
|------|------|------|----------|
| 交易数 | 44 | 15 | 20 |
| 平均收益 | +3.03% | +5.16% | +0.76% |
| 胜率 | 59.1% | 53.3% | 50.0% |
| 测试盈亏比 | — | — | 1.21 |

研发候选 `zt_evo_g2_m15` 的新结构偏向“允许有限回封博弈，同时惩罚过高开板风险”，不再只是基础因子复刻。
它仍被拒绝，原因是有效信号日 `16 < 40`，且锁定测试胜率 `50% < 52%`。系统输出 0 仓位。

## 当前真正的瓶颈

不是 CPU，也不是“再多跑几百代”，而是连续、无未来污染的历史数据。相同 16 天数据继续进化只会增加过拟合机会。

- `daily_price` 虽有 61 个日期，但只有 34 个日期达到较完整的全市场覆盖，且日期不连续；
- 可构造 T0→T1→T2/T3 可成交标签的涨停信号日只有 16 个；
- 东方财富涨停池接口通常不能可靠回取旧日期，必须从现在开始每日持久化；
- 服务器计算资源充足，但当前会话验证到的行情 HTTP 出口仍不可用；可继续由 Windows 采集后发布。

## 下一阶段路线

| 优先级 | 目标 | 完成定义 |
|--------|------|----------|
| P0 | 连续积累涨停历史 | 先达到 40 个可用信号日；正式评估目标至少 120 日 |
| P0 | 每日自动采集与状态告警 | 交易日缺采、行数异常、日期不连续时明确报警 |
| P1 | 重新运行涨停专项 | 40 日后重跑；任何锁定门槛不因结果难看而放宽 |
| P1 | 模拟盘观察 | 准入后至少 20 个新样本，只记录不下真实订单 |
| P2 | 市场状态分层 | 研究不同情绪/题材轮动环境下的涨停结构，不混成单一平均值 |
| P2 | 用户复盘闭环 | 操作卡 → 实际可成交 → 收益/回撤 → 因子漂移报告 |

## PM 最小验收方式

不需要逐行审阅 AI 代码。每次交付只检查四件事：

1. `uv run python -m cli zt status`：数据日期、信号日、候选和实盘闸门；
2. `uv run pytest -q`：自动化回归结果；
3. 本文件的“当前进度/最新结果/风险”是否同步更新；
4. Git 是否按数据层、计算层、产品层拆成可回退提交。

因子只有同时具备可解释公式、无未来数据的标签、锁定测试、明确操作规则和 0 仓位失败路径，才允许从“研究”升级为“可用”。
