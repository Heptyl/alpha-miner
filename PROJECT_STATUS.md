# Alpha Miner 项目状态看板

> 面向 PM / 项目负责人。状态日期：2026-08-15。只在结论、证据、风险或优先级变化时更新。

## 一句话结论

基础工程、数据发布和远程计算已经可运行；涨停专项已经能产生“因子结构 → 次日条件 → 退出 → 仓位”的操作卡，
但剔除非交易日污染后历史样本只有 13 个可用信号日，最新候选未通过锁定测试，当前产品状态是**研究/观察可用，实盘不可用**。

## 当前进度

| 模块 | 状态 | 可验证证据 | PM 结论 |
|------|------|------------|---------|
| 当日数据采集 | 已完成 | 2026-08-14 本地采集 13,404 行，约 31 秒 | 可日常使用 |
| 数据正确性 | 已修复 | 腾讯成交量/成交额字段与日期校验；历史采集隔离实时源 | 继续做日常抽查 |
| 数据发布 | 已完成 | SQLite backup 校验、原子替换、保留 previous | Windows→服务器链路可用 |
| 远程运行时 | 已完成 | Ubuntu 18.04 离线 Python 3.12；涨停专项远端 5/5 通过 | 计算可放服务器 |
| 通用 IC 进化 | 可续跑 | checkpoint、并发、失败者复苏已完成 | 尚无准入因子 |
| 涨停结构因子 | 已实现 | 6 个注册因子、11 基因质量门控进化、可读公式 | 研究可用 |
| 涨停操作卡 | 已实现 | 入场、退出、仓位、贡献、WATCH/AVOID 闸门 | 当前只能观察 |
| 涨停历史采集闭环 | 待 PM 验收 | 严格采集入口、逐次审计、缺采/异常/断档状态、Windows 双时点任务脚本 | 目标机尚未安装计划任务 |
| 用户入口 | 已实现 | `python -m cli zt collect/daily/status/scan` | 日常只需 1 个主命令 |
| 三角色 AI | 已实现 | 普通 CLI 不强制角色；PM/USER 零确认只读；RD 默认 bypass、可用 `-Safe` 恢复防护 | 显式角色会话内禁止换角色 |
| 自动化验证 | 可用/有存量债务 | 离线 407 passed、2 skipped、7 live deselected；本轮 Python Ruff 通过；全库仍有 372 个既有 Ruff 问题 | 功能回归健康，格式债务后续单列治理 |
| Git 分离交付 | 本轮完成三批 | 数据 `ef40a00`；计算 `e5e2e96`；涨停产品为本次独立提交 | 可分别回退 |

远程同步已修复 robocopy 裸 `data` 排除规则误伤 `src/data` 的问题；排除目录现在锚定项目根路径，
避免服务器同时存在“新计算代码 + 旧 schema”的半更新状态。
Windows 设置的代数/种群/worker 现会校验后转发到 SSH；同一数据库的本机与服务器 8×32 逐代轨迹和最终冠军已一致。

## 最新涨停专项演化结果

2026-08-14 使用 8 代 × 32 候选重跑。质量门控忽略了 2026-04-18、04-25、04-26 三个非交易日污染日期；清理后的数据集为 13 个有效信号日、839 个涨停事件。

| 指标 | 训练 | 验证 | 锁定测试 |
|------|------|------|----------|
| 交易数 | 35 | 15 | 15 |
| 平均收益 | +5.09% | +6.35% | +4.52% |
| 胜率 | 62.9% | 66.7% | 73.3% |
| 测试盈亏比 | — | — | 1.07 |

研发冠军 `zt_evo_g4_x25` 的三段平均收益暂时为正，但锁定测试只有 15 笔、测试盈亏比仅 1.07，
且有效信号日 `13 < 40`，不能准入。可解释基准“首板 + 开板回封 1-3 次 + 次日开盘 `-4%～+4%`”
清理后训练/验证/测试为 +1.88%/-1.78%/+3.17%，验证段已经翻负，说明假设尚不稳定。系统继续输出 0 仓位。

## 当前真正的瓶颈

不是 CPU，也不是“再多跑几百代”，而是连续、无未来污染的历史数据。相同 13 天数据继续进化只会增加过拟合机会。

- `daily_price` 虽有 61 个日期，但排除周末后只有 32 个日期达到较完整的全市场覆盖，且日期不连续；
- 可构造 T0→T1→T2/T3 可成交标签的涨停信号日只有 13 个；
- 历史库曾把周末快照标成交易日；采集器现已拒绝周末请求，进化数据集也会自动忽略已有污染；
- 东方财富涨停池接口通常不能可靠回取旧日期，必须从现在开始每日持久化；
- 历史健康审计现识别出 21 个“全市场行情存在但涨停池缺失”的旧日期，并显示 2026-06-24→2026-08-14 的 51 自然日断档；旧缺口不可假回填，只能保留告警并从启用日连续积累；
- `zt collect` 已能在缺采、行数异常或交易日状态无法确认时返回非零，并把失败保留到同日成功重试；Windows 计划任务安装脚本已交付，但本任务未授权修改外部任务计划，目标机仍需 PM/运维明确启用；
- 可回测历史中封单、首次封板时间和真实换手率覆盖为 0；系统现已自动禁用这些无效基因；
- 服务器计算资源充足，但当前会话验证到的行情 HTTP 出口仍不可用；可继续由 Windows 采集后发布。

## 下一阶段路线

| 优先级 | 目标 | 完成定义 |
|--------|------|----------|
| P0 | 连续积累涨停历史 | 先达到 40 个可用信号日；正式评估目标至少 120 日 |
| P0 | 每日自动采集与状态告警 | 工程闭环待验收；目标机启用后持续观察任务退出码与日志 |
| P1 | 重新运行涨停专项 | 40 日后重跑；任何锁定门槛不因结果难看而放宽 |
| P1 | 模拟盘观察 | 准入后至少 20 个新样本，只记录不下真实订单 |
| P1 | 收敛全库 Ruff 基线 | 单独任务机械修复，功能改动与格式改动分开提交；最终 `ruff check .` 为 0 |
| P2 | 市场状态分层 | 研究不同情绪/题材轮动环境下的涨停结构，不混成单一平均值 |
| P2 | 用户复盘闭环 | 操作卡 → 实际可成交 → 收益/回撤 → 因子漂移报告 |

## PM 最小验收方式

不需要逐行审阅 AI 代码。每次交付只检查四件事：

1. `uv run python -m cli zt status`：数据日期、缺采/异常/断档、信号日、候选和实盘闸门；
2. `uv run pytest -q`：自动化回归结果；
3. 本文件的“当前进度/最新结果/风险”是否同步更新；
4. Git 是否按数据层、计算层、产品层拆成可回退提交。

因子只有同时具备可解释公式、无未来数据的标签、锁定测试、明确操作规则和 0 仓位失败路径，才允许从“研究”升级为“可用”。
