# Alpha Miner 用户一页手册

Alpha Miner 的日常入口只有一条命令：

```powershell
python -m cli
```

它只读展示后台已经计算好的 PAPER 玩法卡，不采集、不联网、不现场回测，也不会调用大模型或修改数据库。没有预计算卡时会明确提示等待后台任务生成。

## 一分钟看完

先看数据日和数据健康，再看动作。数据日不是预期交易日、卡片缺少候选依据或关键行情时，结果必须明确写“数据未具备”；停止使用并反馈，不能自行补数或拿旧结果顶替。

然后按“异常/放弃与风险 → PAPER/准入 → 候选与动作 → 历史证据”的顺序阅读。异常、不可成交和放弃条件优先于排名或收益数字。

输出固定分为“今日计划”和“最近 PAPER 结果”：

| 字段 | 含义 |
|---|---|
| 玩法、行为逻辑 | 这项行为金融假说为什么值得做 PAPER 验证 |
| 数据日 | 候选被冻结的交易日；不是实时选股时间 |
| PAPER/准入 | `未准入` 表示仅模拟记录，不是实盘建议 |
| 候选、为何入选 | 后台按预注册规则冻结的股票和原因，不会用未来数据重选 |
| 明日模拟动作 | 条件成立时怎样记录模拟买入；不成立会自动记录未触发或未成交 |
| 放弃、卖出 | 开盘越界、不可成交等放弃条件，以及预先固定的模拟退出时间 |
| 历史证据 | development 样本、成本后结果与限制；不等于已证明优势 |

`PLANNED` 表示等待下一个成功盘后采集自动结算；`TRIGGERED` 表示已经记录模拟买入并等待固定退出日；`COMPLETED` 表示已有完整 PAPER 结果。系统不会连接券商或自动下单。

## 异常先停用并反馈

玩法卡日期异常、长期没有更新、明确缺数或输出与实际不符时，不在 USER 会话诊断、补采或现场计算。记录数据日、玩法 ID、候选和屏幕原文，提交 `USER_FEEDBACK` 给 PM；正常用户不需要记任何后台命令。

## 使用边界

- 数据日不是预期交易日：停止使用并反馈，不在 USER 会话补采。
- 卡片显示 `NOT_ADMITTED`：只能作为 PAPER 模拟观察。
- 实际开盘不满足卡片条件：按卡片自动记录未成交，不追价。
- 排名第一只代表最符合冻结规则，不代表上涨概率或收益承诺。

## 开发者/后台，USER 无需执行

数据采集、状态诊断、补采、远程计算、PIT 实验、进化、报告和发布都属于 RD/后台流程。旧根入口 `recommend`、`signal`、`strategy`、`query`、`daily`、`backtest`、`drift`、`script`、`replay` 已退役；网页 dashboard、旧每日/小时包装与旧推荐任务安装脚本也已移除。后台只需继续维护 `zt collect/status`、`mine` 和持仓报告；`cli/backtest.py` 暂作为涨停内部计算依赖保留，但不再是根命令。远程 `evolve` 只生成 `DEVELOPMENT_ONLY` 候选，不能宣称正式 holdout 或准入。USER 无需记忆或执行这些命令。

授权维护者确需排查数据闭环或检查已有持仓风险时，可退出 USER 会话后分别使用 `python -m cli zt status` 或 `python -m cli report --brief --holdings "600000,000001"`。股票代码必须整体放在引号中以保留前导零；这些结果只提供诊断或背景，不替代玩法卡准入。

需要开发或维护时，应退出 USER 会话，再由授权 RD 使用：

```powershell
.\scripts\agent.ps1 rd
```

USER Agent 保持只读；任何 commit、push、数据库写入、任务安装、远程发布或准入决定都需要独立授权。
